language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t quyenorion/docker-vuefrontend -f Dockerfile.dev .

script:
  - docker run -e CI=true quyenorion/docker-vuefrontend npm run jest